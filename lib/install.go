package lib

import(
    "fmt"
    //"github.com/davecgh/go-spew/spew"
    //"path/filepath"
    //"os"
)

func (mfst Manifest) CheckInstall() (error) {
    fmt.Println("Checking if manifest may be installed")

    return nil
}

func (mfst Manifest) Install() (error) {
    fmt.Println("Installing manifest")

    return nil
}
